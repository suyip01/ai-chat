## 目标
- 移除 `UserCreateView.jsx` 中的批量导入弹窗逻辑，仅保留单个用户创建表单。
- 在 `UsersView.jsx` 顶部操作区为“添加用户”左侧新增“批量添加”按钮，切换到新建的批量导入页面。
- 新建批量导入页面，参考 `ImportView.tsx` 的交互（上传/解析/预览/导入/完成五步），并统一为当前项目的样式与风格。
- 导入显示字段及模板与单个用户创建保持一致：用户名、昵称、邮箱、密码、聊天额度(次数)、登录后禁用时间(分钟)。

## 代码改动
- 删除 `UserCreateView.jsx` 中批量导入相关状态与 UI：`isImportOpen` 及其依赖、拖拽上传/模板下载/解析表格与底部导入按钮等整段弹窗（参考现有位置 `src/admin/users/UserCreateView.jsx:209-350`）。
- 更新 `UsersView.jsx` 的视图切换：
  - 当前为 `view === 'list' | 'create'`（`src/admin/users/UsersView.jsx:74`），新增 `view === 'import'`。
  - 在顶部操作区（`src/admin/users/UsersView.jsx:79-90`）的“添加用户”按钮左侧增加“批量添加”按钮，点击后 `setView('import')`。
- 新建组件 `src/admin/users/UsersImportView.jsx`：
  - 参考 `yumecrush-admin/components/ImportView.tsx` 的分步状态 `upload | analyzing | preview | importing | completed`，但实现为 `.jsx` 并按本项目样式类（如 `dream-input`、圆角/阴影风格、渐隐动画）进行调整。
  - 上传区：拖拽/点击选择文件，仅支持 `.csv`（避免引入 Excel 解析依赖）；显示文件名与大小。
  - 解析逻辑：沿用现有 CSV 解析器 `parseCsvText` 的思路，支持引号/逗号/换行；校验必填：用户名、密码；可选：昵称、邮箱、聊天额度(次数)、登录后禁用时间(分钟)；数值字段进行合法性校验与默认值处理。
  - 预览表格：展示上述字段；错误与警告高亮；分页或仅显示前若干条（与源页面一致）。
  - 导入执行：逐行调用 `usersAPI.create(payload)`，成功/失败计数与逐行状态标识；完成后显示结果并可返回列表或继续导入。
  - 模板下载：生成 CSV 模板，表头为 `用户名,昵称,邮箱,密码,聊天额度(次数),登录后禁用时间(分钟)`，附一行示例数据。
  - 组件对外：`onCancel` 返回列表；内部直接调用 `usersAPI`，或支持 `onSave` 回调（与 `UsersView` 一致）。
- 在 `UsersView.jsx` 中接入：`view === 'import'` 时渲染 `<UsersImportView onCancel={() => setView('list')} />`。

## 风格与交互对齐
- 颜色/圆角/阴影统一为当前项目使用的样式：
  - 按 `UsersView.jsx` 的 `glass-card`、按钮渐变、圆角 `rounded-2xl/3xl`、阴影 `shadow-lg/shadow-pink-200` 等进行类名替换。
  - 图标用 `lucide-react` 与现有页面一致；按钮与加载态同项目风格（导入中用 `Loader2` 动效）。

## 字段与校验
- 字段：`username`、`nickname`、`email`、`password`、`chatLimit`、`expireAfterMinutes`。
- 校验：
  - 必填：用户名、密码；
  - `chatLimit`、`expireAfterMinutes` 为非负整数，默认 `chatLimit=0`、`expireAfterMinutes=60`；
  - 重名处理：支持“自动改名避免重复”开关（可保留与原逻辑一致）。

## 验证方案
- 手动上传 CSV，预览表格显示解析结果与错误提示；
- 执行导入：成功/失败统计与逐行状态正确更新；
- 返回列表查看新增用户是否出现（`usersAPI.list`）。

## 兼容性与假设
- 为避免新增依赖，初版仅支持 `.csv`；若需 `.xlsx/.xls`，后续可引入解析库并扩展解析层。
- 模板与解析字段与单用户表单一致；若需要调整列顺序或命名，可在导入页集中映射。

请确认以上计划，我将开始实施：删除旧弹窗、添加入口按钮、创建并接入批量导入新页面（按本项目风格）。