## 服务方法
- 在 `src/user/services/userCharactersService.ts` 添加：
  - `createCharacter(payload)`: POST `/user-characters`，返回 `id`
  - 保留现有 `createUserCharacter`（用于 `/user/chat-role`）
  - 复用已存在 `getUserCharacter(id)` 进行轮询检查（读取 `system_prompt` 字段）

## 创建页改造
- 文件：`src/user/components/CreateCharacter.tsx`
- 点击“唤醒角色”时：
  1. 若头像是 `data:image/...`，先上传到 `/api/uploads/avatar`，仅保存路径到 `avatar`
  2. 调用 `createCharacter(payload)` 写入数据库 characters 表
  3. 跳转到新页面 `CharacterProfileAwait`，并传入 `createdId`
- 不创建聊天会话（仅进入详情页）

## 角色详情等待页
- 新建 `src/user/components/CharacterProfileAwait.tsx`（复制自 `CharacterProfile.tsx` 并最小改造）：
  - 底部按钮文案仍为“唤醒角色”，默认禁用；提示“角色创建中，请稍等。”
  - `useEffect` 每 3 秒调用 `getUserCharacter(createdId)`；当 `system_prompt` 非空时（后端检测，前端只返回ture或者false），前端判断false时：
    - 更新按钮为可点击（恢复原样）
    - 展示一条轻提示“角色已就绪”
  - 点击按钮执行 `onStartChat()` 进入聊天窗口

## 路由集成
- 在 `src/user/App.tsx`（或现有路由配置）增加新路由：`/character/:id/await` → `CharacterProfileAwait`
- 创建页在成功后使用路由跳转到上述页面

## 交互与文案
- 按钮禁用样式与提示：灰态、`cursor-not-allowed`、文案“角色创建中，请稍等。”
- 轮询停止：在就绪或退出页面时 `clearInterval`

## 保持兼容
- 草稿保存仍调用 `/user-characters/draft`，不触发 LLM；头像处理逻辑相同（上传文件，字段写路径）
- 现有 `UserCharacterSettings` 不受影响（仍用 `/user/chat-role`）

## 失败处理
- 若 1 分钟仍未就绪，提示“生成较慢，可稍后再试”；按钮仍禁用且继续轮询（可选退路按钮）

确认后我将按以上方案实现：新增服务方法、复制并改造详情页、改造创建页调用与跳转、接入轮询逻辑。